dataset_params: 
# want to use the same control parameters, peg list, options list and fit list
# as those used to collect the dataset which was used to train the networks
  dataset_path: "/scr2/muj_senssearch_dataset10HZ_slide_expactions_20200517/"

decision_params:
  converge_thresh: 5
  num_samples: 100
  policy_number: 0
  constraint_type: 2

logging_params:
  display_bool: 1.0 # 1 if display should be on, 0 if display should be off
  logging_folder: "/scr-ssd/sens_search_logging/model_evaluation/perception/" # Place your logging folder in a seperate location then your local code repository
  run_mode: 0 #Options include: 0 -> development, 1 -> evaluation
  num_trials: 20
  run_notes: "_spring_testing"
  step_by_step: 0.0

model_params:
  use_GPU: True
  force_size: [6, 50]
  action_size: 3
  pose_size: 3
  proprio_size: 12

dataset_keys:
  # rgbd:
  # joint_pos:
  # joint_vel:
  force_hi_freq:
  action:
  # macro_action:
  proprio:
  contact:
  # peg_type:
  # hole_type:
  # fit_type:
  # insertion:

info_flow:
  Options_Sensor:
    model_folder: "/scr-ssd/sens_search_logging/models/202005152152_training_67_fit_wexpanded_action/"
    epoch_num: 2500
  Options_ConfNet:
    model_folder: "/scr-ssd/sens_search_logging/models/202005152152_training_67_fit_wexpanded_action/"
    epoch_num: 2500
  # Options_ConfMat:
  #   model_folder: "/scr-ssd/sens_search_logging/models/202005020835_debugging_3_conf_testing/"
  #   epoch_num: 210
