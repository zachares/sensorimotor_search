control_params:
  workspace_dim: 0.02
  kp: [6, 6, 6]
  control_freq: 10
  tol: 0.0005
  seed: 4321
  movement_horizon: 100
  horizon: 100
  min_length: 20
  shift_bool: 1.0
  noise_std: [0.0, 0.0, 0.0] #0.025 good noise scale

logging_params:
  logging_folder: /scr2/muj_motion_primitive_20200722/ #"/scr2/muj_senssearch_dataset10HZ_SAC_20200706_representation/" #"/scr2/muj_senssearch_dataset10HZ_SAC_20200704_noisy_data/"
  logging_data_bool: 1.0 # 1.0 if the program should be saving data, 0.0 otherwise
  display_bool: 0.0 # 1 if display should be on, 0 if display should be off
  collect_vision_bool: 0.0
  image_size: 32
  collect_depth: 1.0
  camera_name: insertion
  num_trials: 1784
  plus_offset: [ 0, 0, 0.02]

policy_keys: [rel_pos, init_pos, contact, peg_type] #  ['rel_pos_shift', 'pos_params', 'peg_type'] # 
traj_keys: [force_hi_freq, rel_proprio, proprio, action, contact, joint_pos, joint_vel, done, reward] # "image", "point_cloud", "depth", "depth_meters"] # rgbd:

action_dim: 3
state_dim: 10

