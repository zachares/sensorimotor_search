control_params:
  kp: [6, 6, 6]
  control_freq: 10
  tol: [0.0005, 0.0005, 0.001] #should match number of dimensions in action spac
  movement_horizon: 100
  noise_std: [0.0, 0.0, 0.00] #0.025 good noise scale - should match number of dimensions in action spac
  tolerance_bool: True
  horizon_bool: False

task_params:
  constraint_type: 0
  shift_bool: 1.0
  horizon: 100
  plus_offset: [ 0, 0, 0.04] # should match number of dimensions in action space
  workspace_dim: 0.02
  seed: 4321
  selection_mode: 3
  ignore_done: False

logging_params:
  logging_folder: /scr2/muj_motion_primitive_20200722/ #"/scr2/muj_senssearch_dataset10HZ_SAC_20200706_representation/" #"/scr2/muj_senssearch_dataset10HZ_SAC_20200704_noisy_data/"
  logging_data_bool: 0.0 # 1.0 if the program should be saving data, 0.0 otherwise
  display_bool: 0.0 # 1 if display should be on, 0 if display should be off
  collect_vision_bool: 0.0
  image_size: 32
  collect_depth: 1.0
  camera_name: insertion
  num_samples: 150

traj_keys: ["force_hi_freq", "rel_proprio", "proprio", "action", "contact", "joint_pos", "joint_vel"] #, "reward", "done", "policy"] # "image", "point_cloud", "depth", "depth_meters"] # rgbd:
use_cuda: True
action_size: 3
eval: True

info_flow:
  # SAC_Policy: # state observations - baseline-thesis - goood one with state pos reward
  #   model_folder: /scr2/rl_models/unsupervised_thesis/
  #   policy_keys: [encoding, rel_pos_shift]
  #   epoch: 199
  #   mode: 2
  #   constraint: 1
  #   state_size: 73
  # Unsupervised_History_Encoder:
  #   model_folder: /scr-ssd/sens_search_logging/models/20200906_training_13_w_L2/
  #   epoch: 5000
  #   sensor: False
  #   encoder: True
  # StatePosSensor_wConstantUncertainty:
  #   model_folder: /scr-ssd/sens_search_logging/models/20200910_training_4_w_constantuncertainty/
  #   epoch: 5000
  #   sensor: True
  #   encoder: False

  SAC_Policy: # state observations - baseline-thesis - goood one with state pos reward
    model_folder: /scr2/rl_models/unsupervised_state_est_reward_only_thesis/
    policy_keys: [encoding, rel_pos_shift]
    epoch: 199
    mode: 4
    horizon: 3
    state_size: 73
    success_params: [0.423, 0.827, 0.846]
    unconstrained: 0
  Unsupervised_History_Encoder:
    model_folder: /scr-ssd/sens_search_logging/models/20200906_training_13_w_L2/
    epoch: 5000
    sensor: False
    encoder: True
  StatePosSensor_wConstantUncertainty:
    model_folder: /scr-ssd/sens_search_logging/models/20200910_training_4_w_constantuncertainty/
    epoch: 5000
    sensor: True
    encoder: False